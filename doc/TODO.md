# TODO（架构演进与后续功能）

## P0（当前必须优先完成）

- [ ] 真实媒体链路打通。
  - 已完成：Flutter 桌面端通过 `flutter_webrtc` 发起真实屏幕采集与 Answer/ICE 回传。
  - 已完成：Flutter 移动端通过 `flutter_webrtc` 建立真实 `RTCPeerConnection` 并渲染远端视频 Track。
  - 已完成：iOS/macOS Podfile、plugin registrant、`WebRTC-SDK` / `flutter_webrtc` / `path_provider_foundation` 接入打通。
  - 下一步：在真实 macOS / iOS 设备上完成首帧验证，确认系统屏幕录制权限、原生构建、首帧显示三项全部通过。
  - 下一步：会话进入 `streaming` 的条件从“answer 已回传”升级为“真实 PeerConnection/Track 已建立并有首帧或首个可用统计信号”。
- [ ] 将现有端到端 smoke 验证扩展到真实媒体链路。
  - 当前已可验证：注册/登录、设备注册、桌面授权、本地 WS、backend 事件流、信令往返、快照读取、暂停/恢复/终止。
  - 下一步需新增：真实视频轨道建立、首帧可见、断线重连、媒体统计采样。
- [ ] 固化本地联调脚本。
  - 使用 `./scripts/run-e2e-smoke.sh` 做控制链路回归。
  - 增加 Darwin Pod 安装检查，避免 Flutter 侧已接入媒体但原生工程未同步。
  - 增加 CI/本地检查，避免固定 `device_id` 与脏数据导致假失败。

## P1（下一阶段）

- [ ] 鼠标/键盘远程控制协议与权限模型。
- [ ] 跨账号协助流程（邀请、同意、时效授权、审计）。
- [ ] 屏幕共享会话内语音通道（WebRTC Data/Audio Track）。

## P1.5（网络增强）

- [ ] TURN over TLS(443) 支持。
  - 作用：在企业网络或严格防火墙下提高连接成功率。
  - 必要性：仅 UDP/TCP TURN 在部分环境会被阻断，TLS 443 更稳。
  - 备注：需准备域名与证书，并评估带宽成本。

## P2（媒体链路完善）

- [ ] 多屏快照改为更低成本的真实截图流/缩略图缓存优化（当前已能上报真实快照，但仍缺码率/尺寸策略）。

## P2（媒体架构演进）

- [ ] 引入媒体中继服务/SFU（按需）以提升复杂网络和多观看者场景稳定性。
- [ ] 可选录屏留档与合规开关。
- [ ] 动态分辨率策略可视化（实时网络评分 + 手动干预阈值）。
- [ ] 视情况将桌面媒体采集从 Flutter 端下沉到 `desktop-server`，仅在真实链路稳定后再做 Rust 侧采集与编码替换。

## P2（平台能力）

- [ ] 邮件注册/找回密码。
- [ ] MFA 二次验证。
- [ ] 桌面端设备指纹与可信设备策略。

## P3（运维与可观测）

- [ ] Prometheus 指标与 Grafana 看板。
- [ ] 链路追踪（OpenTelemetry）。
- [ ] 自动化回归（协议兼容、会话状态机、网络劣化压测）。
